# admin/ — controllers are reached through router.php only;
# Direct access to PHP files in admin/ is disallowed or forwarded to parent router.php.
Options -Indexes -MultiViews

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^ ../router.php [L,QSA]
</IfModule>
