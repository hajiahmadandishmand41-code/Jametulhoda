# راهنمای نصب

## نصب روی InfinityFree (PHP + MySQL)

1. در کنترل‌پنل InfinityFree یک دیتابیس MySQL بسازید و نام دیتابیس، نام کاربری، رمز و میزبان (مثل `sql304.infinityfree.com`) را یادداشت کنید.
2. فایل‌های پروژه را در `htdocs` آپلود کنید.
3. در مرورگر آدرس `/php/install` را باز کنید (مثلاً `https://yoursite.rf.gd/php/install`).
4. مشخصات MySQL و اطلاعات حساب مدیر را وارد کنید و «شروع نصب» را بزنید. نصب‌کننده فایل خصوصی `config/local.php` و قفل نصب را می‌سازد و طرحوارهٔ `database.mysql.sql` را اعمال می‌کند.
5. از `/admin` وارد پنل شوید.

نکات:
- نصب‌کننده فقط یک‌بار اجرا می‌شود؛ پس از ساخت `config/install.lock` مسیر نصب قفل است.
- آپلود فایل روی میزبان‌های اشتراکی به‌صورت محلی در پوشه `uploads` ذخیره می‌شود و نیازی به S3 نیست.
- اگر سایت را روی HTTP معمولی (بدون SSL) باز می‌کنید، کوکی سشن بدون پرچم Secure صادر می‌شود تا ورود دچار حلقهٔ ریدایرکت نشود؛ برای امنیت کامل، SSL رایگان InfinityFree را فعال کنید.

## نصب و اجرای محلی / Vercel + Neon (PostgreSQL)

- نصب و اجرای محلی: [README](README.md)
- تنظیم Neon، Vercel و S3: [راهنمای Deployment](docs/DEPLOYMENT_FA.md)
- مشکلات قبلی، تست‌های واقعی و کارهای باقی‌مانده: [Audit](docs/AUDIT_FA.md)

از دستورهای نصب قدیمی یا رمزهای نمونه استفاده نکنید. فایل‌های `install.php` و `migrate-sections.php` در ریشه فقط wrapper ابزار CLI هستند و از وب اجرا نمی‌شوند؛ نصب وب واقعی همان `/php/install` است. اعتبارنامه‌های موجود در تاریخچه باید rotate شوند؛ حذف از نسخه فعلی تاریخچهٔ Git را پاک نمی‌کند.
